<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.asynctask.mapper.TaskDetailMapper">
    <!-- 启用二级缓存，使用LRU策略，30秒过期，最多缓存500个对象 -->
    <cache eviction="LRU" flushInterval="30000" size="500" readOnly="true"/>

    <resultMap id="BaseResultMap" type="com.example.asynctask.entity.TaskDetail">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="step_name" property="stepName"/>
        <result column="step_order" property="stepOrder"/>
        <result column="step_status" property="stepStatus" typeHandler="com.example.asynctask.handler.StepStatusTypeHandler"/>
        <result column="step_result" property="stepResult"/>
        <result column="context_data" property="contextData" typeHandler="com.example.asynctask.handler.JsonTypeHandler"/>
        <result column="error_message" property="errorMessage"/>
        <result column="retry_count" property="retryCount"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="duration_ms" property="durationMs"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, task_id, step_name, step_order, step_status, step_result,
        context_data, error_message, retry_count, start_time, end_time,
        duration_ms, create_time, update_time
    </sql>

    <!-- 插入任务明细 -->
    <insert id="insert" parameterType="com.example.asynctask.entity.TaskDetail" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task_detail (
            task_id, step_name, step_order, step_status, step_result,
            context_data, error_message, retry_count, start_time,
            end_time, duration_ms, create_time, update_time
        ) VALUES (
            #{taskId}, #{stepName}, #{stepOrder}, #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler},
            #{stepResult}, #{contextData, typeHandler=com.example.asynctask.handler.JsonTypeHandler},
            #{errorMessage}, #{retryCount}, #{startTime}, #{endTime},
            #{durationMs}, #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID查询明细 -->
    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE id = #{id}
    </select>

    <!-- 根据任务ID查找所有明细 -->
    <select id="findByTaskIdOrderByStepOrderAsc" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE task_id = #{taskId}
        ORDER BY step_order ASC
    </select>

    <!-- 根据任务ID和步骤名称查找明细 -->
    <select id="findByTaskIdAndStepName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE task_id = #{taskId}
        AND step_name = #{stepName}
    </select>

    <!-- 根据任务ID和步骤顺序查找明细 -->
    <select id="findByTaskIdAndStepOrder" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE task_id = #{taskId}
        AND step_order = #{stepOrder}
    </select>

    <!-- 根据任务ID和状态查找明细 -->
    <select id="findByTaskIdAndStepStatus" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE task_id = #{taskId}
        AND step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler}
        ORDER BY step_order ASC
    </select>

    <!-- 统计任务各状态步骤数量 -->
    <select id="countStepStatusByTaskId" resultType="java.lang.Object" parameterType="java.lang.String">
        SELECT step_status, COUNT(*)
        FROM task_detail
        WHERE task_id = #{taskId}
        GROUP BY step_status
    </select>

    <!-- 获取任务的下一步骤 -->
    <select id="findNextStep" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM task_detail
        WHERE task_id = #{taskId}
        AND step_order > #{currentStepOrder}
        ORDER BY step_order ASC
        LIMIT 1
    </select>

    <!-- 更新明细 -->
    <update id="update" parameterType="com.example.asynctask.entity.TaskDetail">
        UPDATE task_detail SET
            step_name = #{stepName},
            step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler},
            step_result = #{stepResult},
            context_data = #{contextData, typeHandler=com.example.asynctask.handler.JsonTypeHandler},
            error_message = #{errorMessage},
            retry_count = #{retryCount},
            start_time = #{startTime},
            end_time = #{endTime},
            duration_ms = #{durationMs},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新步骤状态 -->
    <update id="updateStepStatus">
        UPDATE task_detail SET
            step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新步骤状态和结果 -->
    <update id="updateStepStatusAndResult">
        UPDATE task_detail SET
            step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler},
            step_result = #{stepResult},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 更新步骤状态、结果和上下文数据 -->
    <update id="updateStepStatusResultAndContext">
        UPDATE task_detail SET
            step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler},
            step_result = #{stepResult},
            context_data = #{contextData, typeHandler=com.example.asynctask.handler.JsonTypeHandler},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 增加步骤重试次数 -->
    <update id="incrementRetryCount" parameterType="java.lang.Long">
        UPDATE task_detail SET
            retry_count = retry_count + 1,
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <!-- 根据任务类型和步骤名称查找运行中的步骤数量 -->
    <select id="countByTaskIdsAndStepStatus" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM task_detail
        WHERE task_id IN
        <foreach collection="taskIds" item="taskId" open="(" separator="," close=")">
            #{taskId}
        </foreach>
        AND step_status = #{stepStatus, typeHandler=com.example.asynctask.handler.StepStatusTypeHandler}
    </select>

    <!-- 删除任务的所有明细 -->
    <delete id="deleteByTaskId" parameterType="java.lang.String">
        DELETE FROM task_detail
        WHERE task_id = #{taskId}
    </delete>

    <!-- 根据ID删除明细 -->
    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM task_detail
        WHERE id = #{id}
    </delete>

</mapper>