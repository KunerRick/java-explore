server:
  port: 8080

spring:
  main:
    allow-circular-references: true
  datasource:
    url: jdbc:mysql://localhost:3306/test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&connectionCollation=utf8mb4_unicode_ci
    username: root
    password: ewqazxck
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    include: task-handler-config

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.model
  configuration:
    map-underscore-to-camel-case: true

# 异步任务配置
async:
  executor:
    core-pool-size: 5
    max-pool-size: 10
    queue-capacity: 100
    thread-name-prefix: AsyncTask-
    keep-alive-seconds: 60
    allow-core-thread-timeout: true

logging:
  level:
    com.example: DEBUG
    org.springframework.scheduling: DEBUG